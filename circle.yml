machine:
  environment:
    CI_BUILD_NUMBER: "${CIRCLE_BUILD_NUM}"
    CI_BUILD_URL: "https://circleci.com/gh/${CIRCLE_PROJECT_USERNAME}/${CIRCLE_PROJECT_REPONAME}/${CIRCLE_BUILD_NUM}"
    CI_BRANCH: "${CIRCLE_BRANCH}"
    COVERALLS_REPO_TOKEN: tLVNZ8xg4FLODLjOWefByUUz111F7KlTX
    COVERALLS_SERVICE_JOB_ID: "${CIRCLE_BUILD_NUM}"
    COVERALLS_SERVICE_NAME: CircleCI
  timezone:
    Pacific/Auckland
  node:
    version: 0.10.32
  post:
    - cp joukou-conductor/.circle/riak.conf /etc/riak/riak.conf
    - sudo service riak start
    - sleep 5
